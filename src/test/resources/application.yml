spring:
  datasource:
    druid:
      username: sa
      password:
    url: jdbc:h2:mem:test
    filters: stat,wall,log4j
mybatis:
  mapper-locations: classpath*:sql/**/*Mapper.xml
fsstore:
  url:
  bucket:
logging:
  level:
    cn.amber.common: debug

com:
  amber:
    common:
        sensitive:
          com.amber.common.sensitive.entity.UserDO.phone: abb
          com.amber.common.sensitive.entity.UserDO.password: md5
          com.amber.common.sensitive.entity.UserDO.idCard: sm4
          com.amber.common.sensitive.entity.RoleDO.phone: abb
          com.amber.common.sensitive.entity.RoleDO.idCard: sm4
