spring:
  datasource:
    druid:
      username: sa
      password:
    url: jdbc:h2:mem:test
    filters: stat,wall,log4j
mybatis:
  mapper-locations: classpath*:sql/**/*Mapper.xml
fsstore:
  url:
  bucket:
logging:
  level:
    cn.amber.common: debug

# todo
bronze:
  com:
    amber:
      common:
        sensitive:
          entity.UserDO.phone: abb
          cn.amber.common.sensitive.entity.UserDO.idCard: sm4
